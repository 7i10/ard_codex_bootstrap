---
id: 0010
status: pending
created: 2026-09-10
campaign: adr-cifar10-campaign-v1（plan 0097、`cifar10_r18_adr-s2` の
  model 重み評価 `eval-5709dc2e3701675af39e` が AutoAttack 内の CUDA OOM で失敗。
  同じ落ち方が `r18_trades_adr-s1` と `r18_adr-s1` にもある）
question: AutoAttack の最後にある「10,000 枚を一度に流す」forward を先に
  バッチ化してから 3 本を取り直すか、コードには触れず GPU を専有して
  そのまま取り直すか、走行中の 7 本が終わるまで何もしないか。
options:
  A: `autoattack.py:213` の forward を `autoattack_batch_size` でバッチ化し、回帰テストで数値が完全一致することを示してから、失敗した 3 本を取り直す。GPU 約 5.5–10 時間（取り直しぶんのみ、修正自体は 0 時間）。決まること = 残り約 21 本の AutoAttack 評価からこの OOM 経路が消える。事前規則 = バッチ版と非バッチ版が同一テンソルに対して**予測ラベル列まで完全一致**することをテストで示すこと。一致しなければ A は却下。
  B: コードは変えず、空いた 4090 を 1 本ずつ専有して 3 本を取り直す。GPU 約 5.5–10 時間＋ GPU 専有の待ち時間。決まること = その 3 本の数字だけ。残り 18 本の脆弱性はそのまま。事前規則 = 1 本目が完走したら残り 2 本も同じ専有条件で流す。1 本目がまた OOM したら B を捨てて A へ。
  C: 何もせず、走行中の 7 本が terminal になるまで待ってから判断し直す。GPU 追加 0 時間。決まること = この OOM が「3 本の事故」なのか「同時実行しているかぎり必ず出る」のかが分かる。事前規則 = 7 本のうち 1 本でも同じ OOM で落ちたら A を必須とする。全部完走したら B で足りる。
recommendation: A
chosen: null
---

## 追記（2026-09-10 11:07Z）— Option C の事前規則が発火した

この packet を書いた時点で「走行中だった 7 本」のうちの 1 本、
`cifar10_mobilenetv2_adr-s1` の model 重み評価
（`eval-4615ede05d4f96adb48f`）が、**まったく同じ場所で同じように**落ちた。
Option C の事前規則は「7 本のうち 1 本でも同じ OOM で落ちたら A を必須と
する」だった。したがって **C は選択肢から外れる**。`chosen` は null のまま、
決めるのは人間である。

新しく分かったことは 2 つある。

1. **これは ResNet-18 だけの話ではなく、MobileNetV2 のほうが 1.5 倍危ない。**
   失敗した allocation は **3.66 GiB**、これは
   `10000 × 96 × 32 × 32 × 4 B = 3.662 GiB` とバイト単位で一致する。
   `src/ard/models/registry.py:126` が `mobilenet_v2_cifar` の stem stride を
   `(1,1)` にしているので解像度が 32×32 のまま保たれ、`features[2]` の
   expansion conv（expand_ratio 6）が全 10,000 枚ぶんの **96**×32×32 を
   一度に確保しようとする。ResNet-18 の 2.44 GiB
   （`10000 × 64 × 32 × 32 × 4 B`）と同じ機構で、係数だけが大きい。
   残っている MobileNetV2 の評価は、この欠陥に対して最も脆い部類に入る。

2. **落ちているのは冗長な再計算だけで、AutoAttack 自体は完走している。**
   lane log では Square が `34/34` まで進み、AutoAttack 自身が
   `robust accuracy after SQUARE: 42.84% (total time 7656.5 s)` を出力して
   いる（traceback がログ上で先に見えるのは stderr が先に flush された
   だけ）。つまり `run_standard_evaluation` は正常に返り、その次の 213 行で
   死んでいる。**この 42.84 % は完走しなかった run のログ片であって結果では
   なく、どの record にも report にも表にも入れてはならない。**
   `evaluation-results.json`・`autoattack-best.json`・`completion.json` は
   いずれも書かれておらず、`last.pt` には到達していない。2 時間 8 分
   （08:59:09Z → 11:07:25Z）ぶんの GPU 時間から取り込めるものは何もない。

落ちたときの GPU 0 は 23.52 GiB 中 **2.93 GiB** 空き、プロセスが 5 本
（13.12 + 1.74 + 1.74 + 1.71 GiB ＋ この run の 2.25 GiB）同居していた。

**レーンドライバの second defect も再現した。** lane B は `exit=1` の
3 秒後（20:07:26+09:00）にそのまま `--weights=ema`
（`eval-74a8f5d0042ebea0b0d1`）を開始している。これは前述のとおり別 plan の
インフラ案件で、この packet では決めない。

証拠の全文は plan 0097 の Progress log（2026-09-10 11:07Z のエントリ）。
これで同一 traceback による失敗は **4 本**（`r18_adr-s2`、
`r18_trades_adr-s1`、`r18_adr-s1`、`mobilenetv2_adr-s1`）になった。
取り直しの見積もりは A・B とも 3 本ではなく 4 本ぶん、
**約 7–13 GPU 時間**に増える。

## 追記（2026-09-10 11:21Z）— 5 本目。方法の問題ではないことが確定した

`cifar10_r18_trades_49k_validation-s0` の model 重み評価
（`eval-7f2c13eb1eb778fd2e2a`、lane C）が、**同じ 213 行、同じ 2.44 GiB**
で落ちた。これで同一 traceback による失敗は **5 本**になる。

新しく分かったことは 3 つある。

1. **これは ADR 系の方法とは無関係である。** これまでの 4 本
   （`r18_adr-s2`、`r18_trades_adr-s1`、`r18_adr-s1`、`mobilenetv2_adr-s1`）は
   すべて `adr` 系だったが、今回は `method.adr: null` の素の TRADES である。
   要求サイズは 4 本目までと同じく `10000 × 64 × 32 × 32 × 4 B = 2.44 GiB`。
   確保量を決めているのは**テスト集合の枚数とチャネル数だけ**であり、
   方法・教師・arm のどれでもない。したがって残りの評価は
   **一本残らず**この経路を通る。
   （traceback の最内は今回 `registry.py:74`、これまでは `registry.py:75`。
   同じ `layer1` の BasicBlock 内で、2 つある batch-norm のどちらで
   allocator が尽きるかが違うだけで、科学的な差ではない。）

2. **今回落ちたのは、取り直しが他の 4 本と交換できない 1 本である。**
   arm 6（49k-validation の TRADES pilot）は契約上 **seed 1 本だけ**で、
   これがこの arm の唯一の評価だった。事前登録された比較（公式テストの
   AutoAttack を arm 4 seed 0 の保存済み 47.87 best / 44.99 last と突き合わせる）
   は、この 1 本を取り直すまで**そもそも作れない**。この arm の
   validation PGD は n=1,000 で標準誤差 1.6 pp あり、training postrun の
   エントリはそこから判定を出すことを明示的に拒否している。その拒否は
   今も正しく、欠けている数字はこの失敗のせいである。

3. **失敗の証拠ディレクトリは消える。** 5 本のうち **3 本**
   （`r18_adr-s2`、`r18_trades_adr-s1`、そして 11:09Z 以降に消えた
   `r18_adr-s1`）は既に disk 上に無い。`r18_adr-s1` には
   `evaluation.failed-oom-eval-e2da1d97` への rename も無いので、
   退避ではなく削除である。plan の Progress log に書き写した数字が
   唯一の記録になっている。取り直しの前に必ず `mv` で退避すること。

**露出の実測（11:21Z、event ではなく manifest から直接）。** campaign root に
評価ディレクトリは 18 本。**成功 7 本**（`sync_pending` かつ
`evaluation-results.json` あり）、**失敗して残っている 2 本**、
**実行中 9 本**。この 9 本は全部、2 時間走り切った最後に同じ確保を試みる。
落ちたときの GPU 0 は 23.52 GiB 中 **1.07 GiB** 空きで、5 本のうち最も
きつい（`r18_adr-s1` 1.89 GiB、`mobilenetv2_adr-s1` 2.93 GiB）。
同居プロセスは 3 本（1.74 + 13.13 + 1.54 GiB）＋ この run の 6.02 GiB。

**コストの更新。** 取り直しは 4 本ではなく **5 本**、1 本 1.8–3.3 GPU 時間
として **約 9–16.5 GPU 時間**。今回の run 自体は 2 時間 21 分
（08:59:09Z → 11:20:48Z）を消費して何も残さなかった。
Option A・B の GPU 見積もりはどちらもこの 9–16.5 時間に読み替える。
frontmatter の `options` は書いた時点の「3 本」のままにしてある
（決定文は書き換えない）。**A を推す理由は 1. によって強まった**
——方法にも arm にも依らないと確定した以上、B の「専有して取り直す」は
残り 9 本の実行中ぶんに対して何も効かない。`chosen` は依然 null であり、
決めるのは人間である。

## 追記（2026-09-10 12:52Z）— 7 本目。取り直しても同じ場所で落ちた

`cifar10_r18_trades_49k_validation-s0` の model 重み評価
（`eval-7f2c13eb1eb778fd2e2a`）が、**もう一度**同じ 213 行、同じ 2.44 GiB で
落ちた。11:21Z の追記で「5 本目」として書いたのと同じ run である。あのあと
別のレーン（lane J）が同じコマンドをそのまま流し直し、`20:47:27+09:00` に
始まって `21:52:10+09:00` に `exit=1` で終わった。1 時間 4 分 40 秒。
これで同一 traceback による失敗は **7 本**（6 本目は 11:26Z に記録した
`r18_trades_adr-s0`）。

新しく分かったことは 3 つある。

1. **同じ checkpoint を 2 回評価して、数字が完全に一致した。** 2 回の
   AutoAttack は別プロセス・別の混み具合・所要時間はおよそ半分だったのに、
   initial accuracy **79.85 %**、`APGD-CE 50.76 %`、`APGD-T 47.62 %`、
   `FAB-T 47.62 %`、`SQUARE 47.62 %`、`max Linf perturbation 0.03137`、
   `nan in tensor 0`、`robust accuracy 47.62 %` と、印字された最後の桁まで
   すべて同じだった。Square に入った生き残りの枚数（`38/38`、最後の batch が
   26 枚）まで同じである。違ったのは時間だけ（APGD-CE 161.4→77.3 秒、
   SQUARE 累計 8459.2→3858.0 秒）。
   つまり **AutoAttack 自身が返す robust accuracy は、プロセスをまたいでも
   再現する**。これは Option A にとって追い風である——「バッチ版と非バッチ版が
   予測ラベル列まで完全一致すること」という事前規則は、run 間のばらつきに
   邪魔されずに判定できる。同時に、この packet が意図的に選択肢から外した
   「213 行を消して AutoAttack 自身の数字を採用する」案に対する
   「走るたびに数字が変わるのでは」という反論も、少なくともこの 2 本については
   消える（記録される量が変わるという論点は残るので、外した理由自体は
   変わらない）。plan 0097 の 11:47Z 追記が測った推定量のずれ
   （非バッチ再計算のほうが常に高く出る、最大 0.09 pp）と合わせて読むこと。
   **それでもここでは選ばない。`chosen` は null のままである。**

2. **1 回目の証拠は消えた。** lane J は
   `FileExistsError: refusing to overwrite existing evaluation output` を
   出さずに始まっている。つまり走る前に lane C の失敗ディレクトリが
   削除されていた。`evaluation.failed-*` への退避も無い。11:21Z の追記で
   「取り直しの前に必ず `mv` で退避すること」と書いたが、そうはならなかった。
   これで 7 本のうち **4 本**の失敗ディレクトリが失われ、残っているのは
   lane log だけである。訓練の checkpoint（`best.pt`、`last.pt`、
   `epoch-{049,099,149,199}.pt`）は無事なので、取り直し自体は今でもできる。

3. **待たずに取り直すと、そのぶん丸ごと無駄になることが実測された。** 今回の
   GPU 0 は 23.52 GiB 中 **688 MiB** 空き、同居は 14.41 GiB のプロセス 1 本
   だけだった。7 本の空きメモリは
   0.37 < **0.67（今回）** < 1.07 < 1.89 < 2.93 GiB。長く走ったかどうかも、
   極端に混んでいるかどうかも関係なく、**最後の一歩で 2.5 GiB 前後の余裕が
   無ければ落ちる**。Option B の「専有して取り直す」の事前規則は
   「1 本目がまた OOM したら B を捨てて A へ」だったが、今回は専有していない
   ので厳密にはこの規則は発火していない。発火していないというだけで、
   専有しない取り直しには意味が無いことは示された。

**コストの更新。** 取り直しが要る run は 5 本から **6 本**になった
（`r18_adr-s1`、`r18_adr-s2`、`r18_trades_adr-s1`、`mobilenetv2_adr-s1`、
`r18_trades_49k_validation-s0`、それに lane K で走っている
`r18_trades_adr-s0` の再取得もまた OOM すればこれも）。1 本 1.8–3.3 GPU 時間
として **約 11–20 GPU 時間**。ただし今回は 1 時間 5 分で落ちており、空いた
GPU なら下限はもっと低い可能性がある。今回の失敗そのものは 1 時間 4 分 40 秒を
消費して何も残さず、この arm-seed 1 本に費やした合計は 3 時間 26 分になった。

**この packet が `chosen: null` のまま取り直しが走っている。** 上の lane J と、
plan 0097 の 12:02Z 追記にある lane K の再取得がそれである。どちらもこの
packet を決めずに始まっており、lane J は証拠を消したうえで失敗した。
取り直しを続けるのか止めるのかも含めて、決めるのは人間である。

証拠の全文は plan 0097 の Progress log（2026-09-10 12:52Z のエントリ）。

## 追記（2026-09-10 14:05Z）— 8 本目。3 回目も同じ場所で落ちた

`cifar10_r18_trades_49k_validation-s0` の model 重み評価
（`eval-7f2c13eb1eb778fd2e2a`）が、**3 回目**も同じ 213 行、同じ 2.44 GiB で
落ちた。11:21Z の追記が「5 本目」、12:52Z の追記が「7 本目」として書いたのと
同じ run である。`21:56:22+09:00` に始まり `23:01:41+09:00` に `exit=1`
（`canon-eval-lane-J.log:266,528`）、manifest では
`created_at 12:56:24.364Z` → `finished_at 14:01:40.932Z` = **1 時間 5 分 16 秒**。
traceback は `canon-eval-lane-J.log:432-492`、最内は `registry.py:75`。
これで同一 traceback による失敗は **8 本**になる。

新しく分かったことは 3 つある。

1. **lane J は正しく止まっていた。12:52Z と 13:05Z の読みは間違っていた。**
   両方の追記・エントリは、ログに `eval-start` が 2 回続くのを見て
   「lane J はレーンドライバの auto-continue 欠陥を持つ」と読んだ。実際は逆で、
   `canon-eval-lane-J.log:264-265` は `21:52:10+09:00` に
   `[lane-J][STOPPING] non-zero exit, not auto-continuing to next queued job`
   と `LANE_J_DONE` を出しており、ドライバは正しく終了している。3 回目
   （`…:266`、`21:56:22+09:00`）は、その **4 分後に同じコマンドが別途もう一度
   流された**もので、同じログファイルに追記されただけである。したがって
   lane J は lane B・E・F の欠陥の例ではない。この訂正は責任の所在を変える
   ——「ドライバが止まらなかった」ではなく、**この packet が `chosen: null` の
   まま、3 回目が意図的に流された**ということである。

2. **backlog A8 はこの失敗を防げない。A8 と Option A は代替ではなく補完である。**
   allocator のメッセージが不足の両側を名指ししている。落ちたプロセスの内側で
   **reserved but unallocated は 58.34 MiB しかない**（in use 8.40 GiB のうち
   7.88 GiB が PyTorch の allocated）。つまり `src/ard/cli/evaluate.py` に
   `torch.cuda.empty_cache()` を入れる——A8 の内容はこれだけである——ことで
   取り戻せるのは **2.44 GiB の要求に対して最大 58 MiB** にすぎない。A8 が
   効くのは**同居プロセスの側**で、今回の同居は PID 2314500 の **13.71 GiB**、
   `docs/debugging/0029-adr-cifar10-eval-oom-cascade.md` が書いている
   13〜15 GB の膨張パターンそのものである。A8 は供給側を縮め、この packet の
   Option A は需要側を縮める。**どちらか一方で 2.44 GiB の穴が閉じる保証は
   ない。**「A8 で packet 0010 の OOM は直る」と読んではいけない。

3. **3 回目も数字が最後の桁まで一致した。** initial accuracy **79.85 %**、
   `APGD-CE 50.76 %`、`APGD-T 47.62 %`、`FAB-T 47.62 %`、`SQUARE 47.62 %`、
   `robust accuracy 47.62 %`、`max Linf perturbation 0.03137`、
   `nan in tensor 0`、Square に入った枚数の batch 構成（`38/38`、最後が 26 枚）
   まで、1 回目・2 回目と同一である。違ったのは時間だけ（2 回目 → 3 回目で
   APGD-CE 77.3→76.5 秒、APGD-T 573.5→606.1 秒、FAB-T 2094.3→2166.2 秒、
   SQUARE 3858.0→3894.8 秒）。**AutoAttack 自身が返す robust accuracy は
   3 プロセスにわたって再現した。** Option A の事前規則（「予測ラベル列まで
   完全一致」）は run 間のばらつきに邪魔されずに判定できる、という 12:52Z の
   論点がさらに強まる。

**混み具合は中程度だった。** GPU 0 は 23.52 GiB 中 **1.39 GiB** 空き、同居は
1 本のみ。この packet が記録してきた空きメモリ（0.37 / 0.67 / 1.07 / 1.89 /
2.93 GiB）の中で 4 番目、1.07 と 1.89 の間に入る。12:52Z の結論は変わらない
——**最後の一歩で 2.5 GiB 前後の余裕が無ければ落ちる**、それだけが効いている。
**未解決の不整合を 1 つ指摘しておく。** 1 本目の空きメモリを、plan の 11:01Z
エントリは **155 MiB**、この packet の一覧は **0.37 GiB** としている。両方が
同じ読みであることはあり得ない。この packet を整理する人はどちらが正しいか
確認すること。ここでは直さない。

**証拠。** 3 回目は `FileExistsError` を出さずに始まっており、**2 回目の失敗
ディレクトリも削除されていた**（この run の `wandb/` には 3 回目の
`offline-run-20260910_215625-…` しか無い）。これで 8 本のうち **5 本**の失敗
ディレクトリが失われ、残っているのは lane log だけである。今回のディレクトリ
自体は残っている。取り込めるものは無い——`evaluation-results.json`・
`autoattack-best.json`・`completion.json` はいずれも書かれておらず、
`last.pt` には到達していない。上に引いた `robust accuracy 47.62 %` は
**完走しなかった run のログ片であって結果ではなく、どの record にも report にも
表にも入れてはならない。** 訓練の checkpoint は無事なので取り直しは今でも
できる。

**コストの更新。** 今回は 1 時間 5 分 16 秒を消費して何も残さなかった。この
arm-seed 1 本に費やした合計は 3 回で **約 4 時間 32 分**（2 時間 21 分 42 秒
＋ 1 時間 4 分 40 秒 ＋ 1 時間 5 分 16 秒）。取り直しが要る run は 6 本で
変わらず（この run は既にその 6 本に入っている）、**約 11–20 GPU 時間**の
見積もりも変わらない。

**`chosen` は依然 null であり、決めるのは人間である。** この postrun は
何も起動していない。証拠の全文は plan 0097 の Progress log
（2026-09-10 14:05Z のエントリ）。

## 追記（2026-09-10 15:21Z）— 9 本目。取り直しが取り直しを殺した

`cifar10_r18_adr-s1` の model 重み評価（`eval-e2da1d97fcd3e5932fab`、lane M）が、
**同じ 213 行、同じ 2.44 GiB** で落ちた。11:21Z の追記で「3 本目」として書いた
のと同じ arm-seed の **2 回目**である。`23:07:28+09:00` に始まり
`00:14:56+09:00` に `exit=1`（`canon-eval-lane-M.log:1,269`）、manifest では
`created_at 14:07:30.459Z` → `finished_at 15:14:55.280Z` = **1 時間 7 分 25 秒**。
これで同一 traceback による失敗は **9 本**になる。

新しく分かったことは 3 つある。

1. **メモリを奪った相手の名前が分かった。しかも、この packet が「取り直すな」
   と書いたもう一方の run である。** allocator のメッセージが名指しした
   **PID 2388375（13.71 GiB）** は、この postrun の時点でもまだ生きており、
   プロセステーブルがその正体を示している——
   `ard.cli.evaluate … cifar10_r18_trades_49k_validation-s0 … --weights model`、
   親は `run_eval_lane.sh J 0`、いま GPU 0 上で **14042 MiB**。つまり
   **すでに 3 回同じ落ち方をしている run の 4 回目**である。
   `chosen: null` のまま流された取り直しが、`chosen: null` のまま流された
   別の取り直しを餓死させた。落ちた側の GPU 0 の空きは 23.52 GiB 中
   **1.39 GiB**。この packet が記録してきた空き（0.37 / 0.67 / 1.07 / 1.39 /
   1.89 / 2.93 GiB）の下から 4 番目で、**最後の一歩で 2.5 GiB 前後の余裕が
   無ければ落ちる**という 12:52Z の結論はそのままである。

2. **A8 では塞がらないことの 3 つ目の実測。** 落ちたプロセスの内側で
   **reserved but unallocated は 58.35 MiB**（in use 8.40 GiB のうち 7.88 GiB が
   PyTorch の allocated）。14:05Z の追記が別の run で測った 58.34 MiB、
   この run の 1 回目が測った **109.66 MiB**（`canon-eval-lane-D.log:227`）と
   合わせて 3 つ、どれも 2.44 GiB に対して 2 桁足りない。
   `torch.cuda.empty_cache()`（＝ A8 の内容のすべて）が取り戻せるのは
   犠牲者の内側の数十 MB であり、GiB は隣人が握っている。
   **A8 と Option A は代替ではなく補完である**という 14:05Z の結論は動かない。

3. **印字される AutoAttack の数字が、2 本目の arm-seed でもプロセスをまたいで
   完全一致した。** 1 回目（lane D、`17:59:07+09:00` → `20:04:47+09:00`、
   **2 時間 5 分 40 秒**）と 2 回目（lane M、上記）は、
   initial accuracy **82.82 %**、`APGD-CE 52.78 %`、`APGD-T 48.66 %`、
   `FAB-T 48.65 %`、`SQUARE 48.65 %`、`robust accuracy 48.65 %`、
   `max Linf perturbation 0.03137`、`nan in tensor 0`、そして Square に入った
   batch 構成（`39/39`、最後が 1 枚）まで、印字された最後の桁まで同一だった。
   違ったのは時間だけ（APGD-CE 194.6→101.3 秒、APGD-T 1371.2→757.1 秒、
   FAB-T 4093.4→2257.3 秒、SQUARE 7490.2→4015.4 秒）。
   これまでこの再現性は `r18_trades_49k_validation-s0` の 3 プロセスでしか
   示されていなかったが、**別の arm-seed、別の方法（素の TRADES ではなく ADR）**
   でも成立した。Option A の事前規則——「バッチ版と非バッチ版が予測ラベル列まで
   完全一致すること」——は run 間のばらつきに邪魔されずに判定できる、という
   12:52Z・14:05Z の論点はこれで 2 例目の裏づけを得た。
   **それでもここでは選ばない。`chosen` は null のままである。**

**この `48.65 %` は完走しなかった run のログ片であって結果ではなく、どの record
にも report にも表にも入れてはならない。** `evaluation-results.json`・
`autoattack-best.json`・`completion.json` はいずれも書かれておらず、`last.pt`
には到達していない。ディレクトリに残っているのは `panel-best.jsonl`、
`sample-stats-best.parquet`、`resolved_evaluation_config.yaml`、
`evaluation-lineage.json` と `run-bundle/` の 6 ファイルだけである。

**11:05Z に書いた取り直しコマンドは、前提を 2 つとも外して流された。** あの
コマンドは `evaluation.failed-oom-eval-e2da1d97` への `mv` から始まり、
「空いている 4090 で流さなければ、また 2 時間後に同じ場所で落ちる」と明記して
いた。どちらも守られていない。lane M は `FileExistsError` を出さずに始まって
おり、ディレクトリには `offline-run-20260910_230731-…` の W&B segment しか
無い——**1 回目の証拠ディレクトリは退避ではなく削除された**。11:21Z の追記が
止めようとした損失がまた起きたということである。1 回目の数字が残っているのは、
`canon-eval-lane-D.log` がディレクトリと一緒に消されなかったからにすぎない。
**今回のディレクトリは無事である。消さないこと。**

**コストの更新。** 今回は 1 時間 7 分 25 秒を消費して何も残さなかった。この
arm-seed 1 本に費やした合計は 2 回で **約 3 時間 13 分**。取り直しが要る run は
6 本から **5 本**に減った——`r18_trades_adr-s0` は lane K の再取得が
`23:06:03+09:00` に `exit=0` で完走したためである。残るのは `r18_adr-s1`、
`r18_adr-s2`、`r18_trades_adr-s1`、`mobilenetv2_adr-s1`、
`r18_trades_49k_validation-s0` の 5 本、1 本 1.8–3.3 GPU 時間として
**約 9–16.5 GPU 時間**。

**`chosen` は依然 null であり、決めるのは人間である。** この postrun は何も
起動していないし、何も止めていない。証拠の全文は plan 0097 の Progress log
（2026-09-10 15:21Z のエントリ）。

## 追記（2026-09-10 15:46Z）— 初めて、落ちた評価が取り直しで完走した

`cifar10_r18_trades_49k_validation-s0` の model 重み評価
（`eval-7f2c13eb1eb778fd2e2a`）が、**4 回目の試行で `exit=0` で完走した**。
この packet が追ってきた失敗のうち、取り直して最後まで通ったのは
**これが初めて**である。数字は plan 0097 の 15:46Z エントリにある。

分かったことは 3 つある。どれも証拠であって、選択ではない。

1. **コードを 1 行も変えずに通った。** 1〜3 回目（2 時間 21 分 42 秒、
   1 時間 4 分 40 秒、1 時間 5 分 16 秒）はすべて `autoattack.py:213` の
   同じ 2.44 GiB で落ちた。4 回目は `23:06:57+09:00` → `00:43:48+09:00` の
   **1 時間 36 分 51 秒**で、同じ source SHA の同じコマンドである。
   変わったのは混み具合だけで、これは 12:52Z の追記が出した読み
   ——「最後の一歩で 2.5 GiB 前後の余裕があるかどうかだけが効く」——
   がそのまま当たった形になる。
   つまり **「取り直しだけでこの欠陥は抜けられる」ことの実例が初めて出た**。
   これは Option B に有利な証拠である。ただし今回も GPU を専有していないので、
   B の事前規則（「1 本目が完走したら残り 2 本も同じ専有条件で流す」）は
   厳密には発火していない。**したがってここでは選ばない。**
   なお、この arm-seed 1 本に費やした合計は **約 6 時間 8 分**になり、
   そのうち **約 4 時間 32 分は何も生まなかった**。

2. **Option A の事前規則を、初めて同一プロセス内の数字に当てられる。**
   完走した run では、AutoAttack 自身が返した robust accuracy が
   best **47.62 %** / last **46.32 %**、これに対して本プロジェクトの
   非バッチ再計算が記録した値は best **47.64** / last **46.37** だった。
   差は **+0.02 pp と +0.05 pp**、10,000 枚のうち **2 枚と 5 枚**である。
   どちらも正の向きで、11:47Z のエントリが測った「非バッチ側が常に高く出る、
   最大 0.09 pp」の範囲に収まる。
   これまでは AutoAttack 側の数字しか（落ちた run の log 断片としてしか）
   取れていなかった。Option A の事前規則は「バッチ版と非バッチ版が
   **予測ラベル列まで完全一致**すること」なので、accuracy が 2 枚・5 枚ぶん
   ずれているという事実は、その規則を立てる際の出発点になる。
   **これは A を却下する根拠ではない**——ここで比べているのはバッチ版と
   非バッチ版ではなく、AutoAttack 自身の推定量と非バッチ再計算という
   別の 2 つだからである。A の回帰テストが比べるべき対象は依然として前者であり、
   その測定はまだ誰もしていない。記録として残す。

3. **best checkpoint の数字が 4 プロセスで再現した。** 4 回目も
   `initial accuracy: 79.85 %`、`APGD-CE 50.76 %`、`APGD-T 47.62 %`、
   `FAB-T 47.62 %`、`SQUARE 47.62 %`、batch 構造（`square - 38/38 - 0 out of 26`）
   まで 1〜3 回目と最後の桁まで同じだった。動いたのは所要時間だけである。
   14:05Z の追記は「1〜3 回目の `47.62 %` は完走しなかった run の log 断片だから
   記録に入れてはならない」と書いた。**その判断は維持する。** 報告してよい値は
   完走した run 自身の **47.64** であり、3 つの断片はそれを傍証するにとどまる。

**取り直しが要る run は 5 本から 4 本に減った**（`r18_adr-s1`、`r18_adr-s2`、
`r18_trades_adr-s1`、`mobilenetv2_adr-s1`）。1 本 1.8–3.3 GPU 時間として
**約 7–13 GPU 時間**。

**そして、この packet が `chosen: null` のまま、また取り直しが 2 本走り出した。**
この job が GPU を空けた **56 秒後**に `eval-e2da1d97fcd3e5932fab`
（`r18_adr-s1`）と `eval-fcaab229ec62f00f2a01`（`r18_trades_adr-s1`）の
bundle が両方 `15:44:43Z` に作られている。前者は 15:21Z の追記が
「走っている」と記録した lane M の試行を**上書きしており**、その試行は
terminal manifest を残さずディスクから消えた。結末は復元できない。
**`chosen` は依然 null であり、決めるのは人間である。** この postrun は何も
起動していないし、何も止めていない。証拠の全文は plan 0097 の Progress log
（2026-09-10 15:46Z のエントリ）。

## 追記（2026-09-10 16:57Z）— 10 本目。同居しているかぎり「落ちやすい」のではなく「入らない」

`cifar10_r18_adr-s1` の model 重み評価（`eval-e2da1d97fcd3e5932fab`、lane O）が、
**同じ 213 行、同じ 2.44 GiB** で落ちた。この arm-seed の **3 回目**である。
`00:44:41+09:00` に始まり `01:52:18+09:00` に `exit=1`
（`canon-eval-lane-O.log:1,269`）、manifest では `created_at 15:44:43.758Z` →
`finished_at 16:52:17.632Z` = **1 時間 7 分 34 秒**。これで同一 traceback による
失敗は **10 本**になる。

新しく分かったことは 3 つある。どれも証拠であって、選択ではない。

1. **4090 を 2 本の ResNet-18 評価で共有しているかぎり、この最後の一歩は
   「落ちやすい」のではなく「入らない」。** allocator 自身が出した数字で
   足し算が閉じる——容量 **23.52 GiB**、空き **1.39 GiB**、同居プロセス
   **13.71 GiB**、自分 **8.40 GiB**（うち PyTorch allocated 7.88 GiB）。
   `13.71 + 8.40 + 2.44 = 24.55 GiB` で、カードを **1.03 GiB** 超える。
   13.71 GiB はこの箱での ResNet-18 の AutoAttack 評価の定常フットプリントで
   あり、2 回目のときの同居プロセス（15:21Z の追記、
   `r18_trades_49k_validation-s0`）**も 13.71 GiB** だった。相手が違っても
   同じ数字になる。MobileNetV2 は同じ行で 2.44 GiB ではなく 3.66 GiB を要求する
   （11:07Z の追記）ので、そちらはさらに入らない。
   これは 12:52Z の「最後の一歩で 2.5 GiB 前後の余裕が無ければ落ちる」を、
   **同居している相手の種類が分かっていれば事前に計算できる形**に変えるもので
   ある。Option B の事前規則にある「専有」は、少なくとも
   **「同じカードに 2 本目の ResNet-18 評価を置かないこと」**まで具体化できる。

2. **2 回目と 3 回目は、バイト単位で同一の allocator 状態で死んだ。**
   空き 1.39 GiB、同居 13.71 GiB、自分 8.40 / 7.88 GiB、
   **reserved but unallocated 58.35 MiB**——15:21Z の追記が 2 回目について
   記録した数字と、1 つ残らず同じである。プロセスが違い、同居している相手も
   違うのに同じになる。**この失敗の状態は確率的ではなく決定的である。**
   58.35 MiB は reserved-but-unallocated の **4 つ目**の実測でもある
   （58.34 / 109.66 / 58.35 / 58.35）。どれも 2.44 GiB に対して 2 桁足りない。
   `torch.cuda.empty_cache()`（＝ A8 の内容のすべて）が取り戻せるのは犠牲者の
   内側の数十 MB であり、GiB は隣人が握っている。
   **A8 と Option A は代替ではなく補完である**という 14:05Z 以来の結論は動かない。

3. **メモリを奪った相手は、この run と同じ秒に同じカードへ並べて起動された
   双子だった。** allocator が名指しした **PID 2417286（13.71 GiB、GPU 0）**は
   この postrun の時点でも生きていて **14038 MiB**、箱の上に評価プロセスは
   ちょうど 2 本しかない。lane P（`cifar10_r18_trades_adr-s1`、
   `eval-fcaab229ec62f00f2a01`）が `00:44:41+09:00`、lane Q
   （`cifar10_mobilenetv2_adr-s1`、`eval-4615ede05d4f96adb48f`）が
   `01:17:10+09:00` の開始なので、PID の順序とメモリ量から
   **PID 2417286 は lane P** である（15:21Z の追記と違い、今回の session では
   `ps` が使えないため、プロセステーブルではなくこの 2 点からの推定である）。
   lane P は lane O と **同じ秒**に **同じ GPU 0** で始まっている。つまり
   これは「たまたま残っていた隣人との競合」ではなく、**キューが 1 枚の 4090 に
   ResNet-18 の AutoAttack 評価を 2 本載せた**結果であり、1 の足し算により
   どちらかは必ず 213 行で死ぬ。**両方ともこの packet が「取り直すな」と
   書いた run であり、両方とも `chosen: null` のまま流された。**
   この postrun はそのどちらも起動していないし、止めてもいない。

**印字された数字は 3 プロセス目でも完全一致した。** initial accuracy
**82.82 %**、`APGD-CE 52.78 %`、`APGD-T 48.66 %`、`FAB-T 48.65 %`、
`SQUARE 48.65 %`、`robust accuracy 48.65 %`、`max Linf perturbation 0.03137`、
`nan in tensor 0`、Square の batch 構成（`39/39`、最後が 1 枚）まで 1・2 回目と
最後の桁まで同一である。違ったのは時間だけで、2 回目と 3 回目はそれもほぼ
同じだった（累積で 111.2 / 767.5 / 2272.3 / 4022.6 秒 対 101.3 / 757.1 /
2257.3 / 4015.4 秒、1 回目は 194.6 / 1371.2 / 4093.4 / 7490.2 秒。これらは
attack ごとの所要ではなく標準評価の開始からの累積で、SQUARE の値が wall clock と
一致する）。`r18_trades_49k_validation-s0` の 4 プロセスに続き、この arm-seed で
**3 プロセス**。Option A の事前規則——「バッチ版と非バッチ版が予測ラベル列まで
完全一致すること」——は run 間のばらつきに邪魔されずに判定できる。
**それでもここでは選ばない。`chosen` は null のままである。**

**この `48.65 %` は完走しなかった run のログ片であって結果ではなく、どの record
にも report にも表にも入れてはならない。** `evaluation-results.json`・
`autoattack-best.json`・`completion.json` はいずれも書かれておらず、`last.pt`
には到達していない。

**そして「今回のディレクトリは無事である。消さないこと」は 30 分もたなかった。**
2 回目のディレクトリは lane O が始まった `15:44:43Z` に、退避ではなく削除された
——残っている W&B segment は `offline-run-20260911_004444-…` だけで、2 回目の
`offline-run-20260910_230731-…` は無い。失われた失敗ディレクトリはこれで
**8 つ目**、指示を出した直後に守られなかったのは **2 回連続**である。
これは 16:16Z の追記が書いた読み——評価の run id が
`sha256(train_run_id:evaluation_hash:checkpoint_set…)`、すなわち**試行の識別子
ではなく科学的同一性の識別子**であり、取り直せば必ず同じパスに落ちる——を
裏づける。担当者の不注意である以上に、**この設計の帰結**である。

**そのうえで、いまディスクに残っている terminal-failed の評価 bundle は、この
1 つだけである。** 16:16Z の追記は「キャンペーン全体に 1 つも残っていない」と
書いた。この bundle の `failure_snapshot.directory_digest` は
`2e3f896a96ea31ef09b4fef595b749ad2e4935080905c291b22f73a771ccabae`（5 ファイル）。
**同じ identity で 4 回目を流せば、これも消える。** Option A の回帰テストを
作るなら、落ちる入力を保存したこの 1 つが要る。退避は人間の操作であり、
この postrun は行っていない。

**コストの更新。** 今回は 1 時間 7 分 34 秒を消費して何も残さなかった。この
arm-seed 1 本に費やした合計は 3 回で **約 4 時間 21 分**（2:05:40 ＋ 1:07:25 ＋
1:07:34）。取り直しが要る run は **4 本**のまま（`r18_adr-s1`、`r18_adr-s2`、
`r18_trades_adr-s1`、`mobilenetv2_adr-s1`）——走行中の 2 本はまだ terminal では
ないためである。1 本 1.8–3.3 GPU 時間として **約 7–13 GPU 時間**。

**`chosen` は依然 null であり、決めるのは人間である。** 証拠の全文は plan 0097 の
Progress log（2026-09-10 16:57Z のエントリ）。

## 追記（2026-09-10 17:26Z）— 同居していた 2 本の結末が両方そろい、生き残ったほうは速度が半分だった

`cifar10_r18_trades_adr-s1` の model 重み評価（`eval-fcaab229ec62f00f2a01`、
lane P）が **`exit=0` で完走した**。この packet の冒頭が「同じ落ち方をした」と
挙げた 3 本のうちの 1 本である。そして 16:57Z の追記が
「同居プロセス **13.71 GiB**」と名指しした相手が、この run である。
**同じ秒に同じ GPU 0 で始まった 2 本の結末が、これで両方そろった**——
片方（lane O）は落ち、片方（lane P）は完走した。16:57Z の追記の足し算
（`13.71 + 8.40 + 2.44 = 24.55 GiB > 23.52 GiB`）が予言したとおり、
**どちらか一方は必ず落ちる**という形である。

新しく分かったことは 3 つある。どれも証拠であって、選択ではない。

1. **同居は「落とす」だけではない。生き残ったほうを約 2 倍遅くする。**
   この run 自身の中で、AutoAttack の 4 段が best → last で
   APGD-CE **109.8 → 71.5 s**、APGD-T **654.4 → 330.9 s**、
   FAB-T **1481.7 → 721.4 s**、SQUARE **1649.3 → 798.3 s**——
   **1.54 / 1.98 / 2.05 / 2.07 倍**、ほぼ一様に 2 倍である。同じプロセス、
   同じカード、同じモデルであり、**仕事量では説明できない**：APGD-CE は
   last のほうが 178 枚**多く**処理して 65 % の時間で終わっており、
   APGD-T / FAB-T / SQUARE の入力枚数差は 1.6–2.9 % しかない。
   説明はタイムラインのほうにある。`finished_at 17:22:32.9Z` から
   SQUARE の累積 1922.1 s を引くと last の AutoAttack 開始は **約 16:50:30Z**、
   lane O が死んだのは **16:52:17.6Z** ——つまり **best の半分は
   ResNet-18 の AutoAttack 評価ともう 1 本同居したまま、last の半分は
   その相手が消えた後**に走っている（MobileNetV2 の lane Q は 16:17Z 以降ずっと
   同居しており、変わったのは ResNet-18 の相手だけである）。
   これは process table ではなく、AutoAttack 自身の累積時刻と bundle の
   timestamp からの**再構成**である（この session に `ps` は無い）。
   **この packet の「コストの前提」は落ちた run の時間しか数えていない。**
   実際にはこの欠陥は二重に払わせている——競争に負けた run は何も返さず、
   勝った run も約 2 倍の時間を使う。Option A の見積もり（約 5.5–10 GPU 時間）は
   取り直しぶんとしては変わらないが、**A を採らなかった場合のコストは
   これまで書いてきたより大きい**。

2. **コードを 1 行も変えずに通った 2 例目。** 1 例目は 15:46Z の
   `r18_trades_49k_validation-s0` である。今回も **GPU を専有していない**
   （lane O と lane Q が同居していた）ので、Option B の事前規則
   「1 本目が完走したら残り 2 本も同じ専有条件で流す」は**今回も発火していない**。
   **したがってここでは選ばない。**
   なお `cifar10_mobilenetv2_adr-s1` の model 重み評価
   （`eval-4615ede05d4f96adb48f`、16:16Z の追記が「証拠物を上書きした」と
   記録したもの）も **17:23:59Z に terminal かつ success** になっている
   （数字は未検証・未記録で、postrun はまだ owed）。
   **取り直しが要る run は 4 本から 2 本に減った**（`r18_adr-s1`、`r18_adr-s2`）。
   1 本 1.8–3.3 GPU 時間として **約 3.6–6.6 GPU 時間**。

3. **Option A の事前規則の出発点が、さらに 2 例増えた。** 完走したこの run では
   AutoAttack 自身の robust accuracy が best **48.44 %** / last **47.65 %**、
   非バッチ再計算が記録した値が **48.45** / **47.68** ——
   差は **+0.01 pp と +0.03 pp**、10,000 枚のうち **1 枚と 3 枚**である。
   同じ arm の seed 0（lane K）も printed 49.17 / 48.10 に対し記録 49.19 / 48.11 で
   **+0.02 / +0.01 pp**。どれも正の向きで、11:47Z と 15:46Z の追記が測った
   「非バッチ側が常に高く出る、最大 0.09 pp」の範囲に収まる。
   **これは A を却下する根拠ではない**——ここで比べているのは
   バッチ版と非バッチ版ではなく、AutoAttack 自身の推定量と非バッチ再計算という
   別の 2 つだからである。A の回帰テストが比べるべき対象は依然として前者であり、
   その測定はまだ誰もしていない。記録として残す。

**`chosen` は依然 null であり、決めるのは人間である。** この postrun は何も
起動していないし、何も止めていない。証拠の全文は plan 0097 の Progress log
（2026-09-10 17:26Z のエントリ）。

## この packet は結果についてではない

plan 0097 の M2/M3 はまだ開いており、`docs/experiments/` に取り込むべき
record は存在しない。実際に何も取り込んでいないし、milestone も一つも
tick していない。この packet が扱うのは `/experiment-postrun` が
`eval-5709dc2e3701675af39e` の失敗を追っている最中に見つかった
**評価コードの欠陥**と、それをどう片付けるかである。

証拠の全文は plan 0097 の Progress log（2026-09-10 11:01Z のエントリ）に
ある。以下は要点だけ。

## 何が起きたか

`src/ard/evaluation/autoattack.py` の 211–213 行はこうなっている。

```python
adversarial = adversary.run_standard_evaluation(images, labels, bs=batch_size)
with torch.no_grad():
    accuracy = model(adversarial).argmax(1).eq(labels).float().mean().item()   # 213
```

`bs=128` は AutoAttack の**内側**では効いているが、その次の行の精度再計算は
それを無視して 10,000 枚を**一度に**student に流す。失敗した allocation は
**2.44 GiB** で、これは `10000 × 64 × 32 × 32 × 4 B = 2.441 GiB`、つまり
ResNet-18 の `layer1` が全件ぶん持つ activation とぴたり一致する
（traceback の最内は `registry.py:75`、`layer1` 内の
`self.bn2(self.conv2(outputs))`）。

つまりピークは 2 時間の仕事の**いちばん最後**、AutoAttack の 4 段階を
全部払い終えたところに来る。しかもその大きさは `autoattack_batch_size` では
なく**テスト集合の枚数**で決まる。落ちた時点で GPU の空きは 23.52 GiB 中
**155 MiB**、同じ 4090 に 3 プロセス（1.73 + 8.46 + 13.13 GiB）が載っていた。
9 レーンの手動評価ドライバが 1 枚の 4090 に評価を複数積むので、この
forward が要求する数 GiB の余裕が取れない。

**失われたもの。** `results.append(...)` は `run_autoattack` が返った後
（`src/ard/cli/evaluate.py:411-421`）、metrics ファイルは checkpoint ループの
後に書かれる。したがって `best.pt` の clean と CE-PGD-20 は計算済みなのに
AutoAttack もろとも捨てられ、`last.pt` には到達すらしていない。取り込める
数字は一つもない。lane log に残っている AutoAttack の途中経過
（initial accuracy 83.32 %、APGD-T 後 48.22 %、FAB-T 後 48.22 %、Square は
2/38 で停止）は**完走しなかった run のログ片であって、結果ではない**。
どの record にも report にも表にも入れてはならない。

**影響範囲は 3 本以上。** `canon-eval-lane-E.log` に
`cifar10_r18_trades_adr-s1` の同一 traceback（`exit=1`、20:01:15+09:00）、
11:05Z の再スキャンでは `cifar10_r18_adr-s1/train/evaluation` も `failed`。

**もう一つの欠陥（別 plan 案件）。** レーンドライバは非 0 終了で止まらず、
`exit=1` の直後にそのまま `--weights=ema` を開始している
（lane-E 20:01:15、lane-F 20:01:19）。両方とも 20:04:24 に SIGTERM
（`exit=143`）で消えた。CLAUDE.md のルール 3 に従い、これはこの packet では
決めず、インフラ欠陥として別 plan に切り出すべきものとして記録するに留める。

**この欠陥が残っている限りの露出。** 現時点で AutoAttack 評価は
model 重みが 13 本（failed 1・実行中 6・未着手 6）、EMA 重みが 8 本
（実行中 1・未着手 7）、合わせて**約 21 本**が未了である。

## コストの前提

このキャンペーンで完走した「両 checkpoint ＋ AutoAttack」1 本ぶんの実測
（すべて他ジョブと同居した状態、run-bundle の `created_at`→`finished_at`）：

| run | 実測 |
|---|---:|
| `r18_pgd_at_nesterov-s0` (model) | 1 h 48 m |
| `r18_pgd_at-s1` (model) | 1 h 50 m |
| `r18_adr-s0` (ema) | 1 h 57 m |
| `r18_adr-s0` (model) | 3 h 15 m |

以下の見積もりはすべて **1 本あたり 1.8–3.3 GPU 時間**から換算した。
専有すれば下限側、あるいはそれより速いはずだが、専有での実測はまだ無い。

## Option A — forward をバッチ化してから 3 本取り直す

- **GPU 見積もり**: 修正 0 時間、取り直し 3 本 × 1.8–3.3 h = **約 5.5–10 GPU 時間**。
- **決まること**: 残り約 21 本の AutoAttack 評価からこの OOM 経路が消える。
  今後の評価が同時実行の込み具合に左右されなくなる。
- **事前規則**: バッチ版と非バッチ版が、同一の adversarial テンソルに対して
  **平均だけでなく予測ラベル列まで完全一致**することを回帰テストで示すこと
  （テストは修正前に落ち、修正後に通ること）。一致しなければ A は却下し、
  B か C に戻る。
- **risks**: これは評価コードへの変更である。ただし epsilon・steps・step size・
  random start・normalization・temperature・schedule・checkpoint 選択・
  評価 attack のどれにも触れない。model は `eval()`、BN は running 統計を
  使うので、バッチ化は数値的に中立であることが期待でき、上の事前規則は
  まさにそれを要求している。**AutoAttack 自身が出す robust accuracy を
  そのまま採用して 213 行を消す**という「もっと安い」案は、記録される量が
  変わるので中立ではない。これは新しい contract であり、この packet の
  選択肢には含めない。

## Option B — コードは変えず、GPU を専有して取り直す

- **GPU 見積もり**: 3 本 × 1.8–3.3 h = **約 5.5–10 GPU 時間**。ただし 1 本ずつ
  4090 を専有するため、他のレーンを止める待ち時間が別途かかる。
- **決まること**: その 3 本の数字だけ。残り 18 本は同じ欠陥を抱えたまま。
- **事前規則**: 1 本目が完走したら、残り 2 本も同じ専有条件で流す。1 本目が
  また OOM したら B を捨てて A に移る。
- **risks**: 2 時間走り切った最後で落ちるので、失敗のコストが高い。専有を
  やめた瞬間に再発する。残り 18 本ぶんの露出（38–60 GPU 時間相当）が
  そのまま残る。

## Option C — 走行中の 7 本が終わるまで何もしない

- **GPU 見積もり**: 追加 **0 時間**（走行中の 7 本は既に走っている）。
- **決まること**: この OOM が「混み合った 3 本の事故」なのか「同時実行して
  いるかぎり必ず出る」のかが分かる。今コードを直しても、走行中の 7 本は
  既に旧コードで動いているので恩恵を受けない、という点でも筋は通る。
- **事前規則**: 7 本のうち 1 本でも同じ OOM で落ちたら A を必須とする。
  7 本とも完走したら B で足りる。
- **risks**: 7 本が終わるまで数時間かかり、その間に落ちた本数だけ 2 時間ずつ
  無駄になる。`r18_adr-s1` が既に 3 本目として落ちている以上、「事故」説は
  すでに弱い。

## なぜ A を推すか

原因が推測ではなく**確定**している点が大きい。要求サイズ 2.44 GiB が
`10000 × 64 × 32 × 32 × 4 B` と桁ではなくバイト単位で一致しており、
traceback の行も特定できている。直し方も一意で、しかも「数値が完全一致
すること」を事前規則にできるので、科学的な中身を一切動かさずに済む。
GPU コストは A も B も同じ 5.5–10 時間で、A だけが残り約 18 本ぶんの
露出（1 本 1.8–3.3 h として 38–60 GPU 時間相当）を同時に消す。

**noise floor について。** ここで測り直そうとしているものは何も無い。
評価は保存済み checkpoint に対して固定 seed（`evaluation_attack: 0`）で
走る決定的な手続きなので、RNG の 1–2 pp という floor はこの判断には
掛からない。むしろ逆で、A の修正後に同じ checkpoint から**違う**
AutoAttack の数字が出たら、それは noise ではなく修正が中立でなかった証拠
であり、上の事前規則がそれを検出する。どの選択肢も「結果」を生まない。

**判断が変わる条件。** 回帰テストで予測ラベル列が一致しなければ A は成立
しないので、その場合は B（専有での取り直し）に落とす。逆に、走行中の 7 本が
全部完走して `r18_adr-s1` の失敗が OOM 以外の原因だったと分かれば、C→B で
十分になる。

## 追記（2026-09-10 16:16Z）— この packet の証拠物がディスク上から消えた

`cifar10_mobilenetv2_adr-s2` の model 重み評価（`eval-f909f7c015b179ed0e22`）の
postrun で、2 つ分かった。数字は plan 0097 の 16:16Z エントリにある。

1. **この欠陥は特定の arm のものではない、という読みが一番きれいな形で確認できた。**
   完走したこの run は `mobilenetv2_adr`——つまり **この packet を開かせた
   `mobilenetv2_adr-s1` と同じ arm、同じ contract** である。それが
   **1 回目の試行で両方の checkpoint を 1 時間 6 分 42 秒で完走した**。
   commit `132be5d` が記録した根本原因（同時実行下のアロケータ断片化であって、
   1 本の arm のメモリ特性ではない）を直接支持する証拠であり、
   「特定の arm だけが危ない」という読みを否定する。

2. **`eval-4615ede05d4f96adb48f` は、もうこの packet が指していた失敗ではない。**
   この job が GPU を空けた **35 秒後**（`16:17:13Z`）に、
   `cifar10_mobilenetv2_adr-s1/train/evaluation` に**同じパス・同じ id で**
   新しい試行が作られ、11:07Z の追記が「まったく同じ場所で同じように落ちた」と
   記録した terminal-failed の bundle を**上書きした**。
   これで、キャンペーン全体に **terminal-failed の評価 bundle が 1 つも
   残っていない**。この packet の中心的な証拠物は、もうディスク上に無い。

   **id が衝突するのは偶然ではなく仕様である。** 評価の run id は
   `src/ard/cli/evaluate.py:321-326` で
   `sha256(f"{train_run_id}:{evaluation_hash}:{checkpoint_set}{weights_suffix}")[:20]`
   として決まる。つまり id は**試行の識別子ではなく、科学的同一性の識別子**で
   あり、同じ identity を取り直せば必ず同じ id・同じ出力ディレクトリになる。
   11:21Z の追記が書いた「取り直す前に必ず `mv` する」という指示が守られないと
   証拠が消えるのは、担当者の不注意である以上に**この設計の帰結**である。
   失われた失敗ディレクトリはこれで 7 つ目になる。

   これは Option A に直接効く。A の事前規則は「バッチ版と非バッチ版が予測ラベル列
   まで完全一致することを回帰テストで示す」ことだが、**落ちる入力を保存した
   bundle が 1 つも残っていない**ので、A を作るなら先に「次に落ちた 1 本を
   意図的に保全する」ことが要る。**ここでは選ばない。** `chosen` は依然 null で
   あり、決めるのは人間である。この postrun は何も起動していないし、何も
   止めていない。

## 追記（2026-09-10 18:32Z）— 証拠物が 2 度目に消えた。そして arm 8 は 5 本連続で完走した

`cifar10_mobilenetv2_adr-s0` の model 重み評価（`eval-750b3422e05de8434c22`）の
postrun で 3 つ分かった。数字は plan 0097 の 18:32Z エントリにある。

1. **16:16Z の追記のあとに新しくできた証拠物も、もう消えている。**
   16:16Z の追記が「terminal-failed の bundle が 1 つも残っていない」と書いた
   あと、`r18_adr-s1` が 3 回目の OOM で落ちて（plan 0097 の 13:03Z エントリ、
   commit `32b0d70`）、17:27Z エントリはその
   ディレクトリを「**この packet の唯一残っている証拠物**」と呼んだ。
   その bundle も、いま無い。今回の job が GPU を空けた **122 秒後**
   （`18:31:44.976Z`）に、同じパス・同じ id で **4 回目の試行**
   （`eval-e2da1d97fcd3e5932fab`）が作られ、上書きした。いまその
   ディレクトリにあるのは進行中の 4 回目のファイルだけで、
   `completion.json` も `error-marker.txt` も無い。失われた失敗ディレクトリは
   **8 つ目**である。11:21Z の追記が書いた「取り直す前に必ず `mv` する」という
   指示は、**2 度目も守られなかった**。

   ただし **traceback 自体は残っている**：`canon-eval-lane-*.log` の
   **9 本**が `CUDA out of memory` を含む。つまり失われたのは「落ちた入力を
   保存した bundle」であって、落ち方の記録ではない。Option A の事前規則
   （バッチ版と非バッチ版が予測ラベル列まで一致することを回帰テストで示す）
   には bundle が要るので、16:16Z の追記の結論は変わらない——**A を作るなら
   先に「次に落ちた 1 本を意図的に保全する」ことが要る**。

2. **この packet を開かせた arm が、いま 5 本連続で完走している。**
   `mobilenetv2_adr` の model 重み評価は 3 seed すべて（s0 が今回、s1 は
   16:17:13Z の取り直しが完走、s2 は 16:16Z の追記）、EMA 重み評価は 2 本
   （s1、s2）が terminal かつ success である。s0 の EMA だけが走行中。
   **この arm で 5 本中 5 本が完走した**というのは、16:16Z の追記が
   1 本で示した「特定の arm のメモリ特性ではなく、同時実行下の
   アロケータ断片化である」という読み（commit `132be5d`）を、
   さらに強く支持する。

3. **専有できているときのコストが、はっきり測れた。**
   今回の run は `created_at 17:22:55.823Z` → `finished_at 18:29:42.603Z` で
   **1 時間 6 分 46.8 秒**。うち AutoAttack 自身が報告した stage 時間の合計が
   3,968.4 秒（1 時間 6 分 8 秒）で、**壁時計の 99.0 %** を占める。2 つの
   checkpoint の stage 時間はほぼ同一（63.4 / 441.6 / 1122.1 / 1986.2 秒 対
   63.7 / 432.4 / 1117.9 / 1982.2 秒、最大でも 2.1 % 差）で、つまりこの run は
   最後まで GPU を専有できていた。17:26Z エントリの arm 5 seed 1 が、隣の job
   が死んだ前後で last checkpoint だけ 1.54–2.07 倍速くなったのと正反対である。
   arm 8 seed 2 の 1 時間 6 分 42 秒、seed 1 の EMA の 1 時間 6 分 25.6 秒と
   合わせると、この arm の AutoAttack は**専有できていれば
   ≒1 時間 6 分 30 秒 ± 20 秒で安定する**。これは Option B（コードは変えず
   GPU を専有して取り直す）の所要時間見積りを、推定ではなく実測で置き換える。

`chosen` は依然 null であり、決めるのは人間である。この postrun は何も
起動していないし、何も止めていない。

## 追記（2026-09-10 19:45Z）— この packet を開かせた当の run が、4 回目で完走した

`cifar10_r18_adr-s1` の model 重み評価（`eval-e2da1d97fcd3e5932fab`、lane T）が
**`exit=0` で完走した**。この run は、この packet の 10 件の失敗のうち **3 件**
を 1 本で出した run である（lane D・M・O、いずれも `autoattack.py:213` で
`CUDA out of memory`）。数字は plan 0097 の 19:45Z エントリにある。

分かったことは 3 つある。どれも証拠であって、選択ではない。

1. **Option B が、いちばん落ちていた run の上で、最初から最後まで実演された。**
   `created_at 18:31:44.975Z` → `finished_at 19:35:54.040Z` で **1 時間 4 分 9 秒**。
   コードは 1 行も変えていない。変わったのは **同じカードに何が載っていたか**
   だけである。つまり Option B（コードは変えず GPU を空けて取り直す）は、
   もう「見積り」ではない——**失敗を 3 回出した当の入力で、1 時間で結果が返る
   ことが確かめられた**。ちなみに 3 回の失敗に費やした GPU 時間は合計
   約 4 時間 21 分で、成功したこの 1 本の 4 倍以上である。

2. **専有できていたことが、run の内部だけで確かめられる。**
   AutoAttack 自身の stage 時間の合計 3,816.7 秒が壁時計の **99.2 %** を占め、
   2 つの checkpoint の 4 stage を「その stage に実際に入った入力数」で割ると
   **6.65 / 65.6 / 155.1 / 171.5 ms** 対 **6.66 / 65.4 / 154.8 / 171.1 ms**——
   **4 stage すべてで差 0.3 % 以下**である。これはこの campaign で測れた中で
   いちばん平坦な profile で、17:26Z の追記が測った arm 5 seed 1 の
   **専有側**（隣が死んだあとの 1,922.1 秒）と同じ速度域であり、
   **同居側**（3,895.2 秒）ではない。

3. **AutoAttack の評価が 2 本同時に走っても、それだけでは危険ではない。**
   `eval-6b0db6d5fc98a962f227`（`mobilenetv2_adr-s0`、EMA 重み）が
   18:29:45Z → 19:35:32Z の **1 時間 5 分 47 秒** で、この run と壁時計上
   ほぼ完全に重なって走った。しかし **どちらも遅くなっていない**——後者は
   18:32Z の追記が測った arm 8 の専有時間 ≒1 時間 6 分 30 秒 の 1 分以内、
   前者は上のとおり専有速度である。**遅くならなかった以上、2 本は同じカードに
   載っていない**。この packet の 10 件の OOM も、17:26Z の「2 倍遅くなる」も、
   すべて **1 枚の device に 2 本以上**が載った場合の話である。したがって
   Option B が要求するのは「同時実行をやめること」ではなく
   **「1 枚あたり 1 本にすること」**である。

   なお **これは AutoAttack の累積時間マークと bundle の timestamp からの
   再構成であって、process table からの読み取りではない**。この session に
   `ps` も `nvidia-smi` の履歴も無い。

**推定量の照合は 16 件になった。** 印字された robust accuracy と
`src/ard/evaluation/autoattack.py:213` の非バッチ再計算値の比較は、この run で
`best.pt` **48.65 % 対 48.65 %（初の完全一致）**、`last.pt` **44.81 % 対
44.83 %**（+2 枚、+0.02 pp）。通算 **16 件中 12 件が不一致で、12 件すべて同じ
向き**（記録側が高い）、最大幅は依然 **0.09 pp** である。

**新しい運用上の事実が 1 つある（この packet の対象ではないが、この packet の
証拠が消え続ける理由に直結する）。** headless postrun が動いていない。
`mobilenetv2_adr-s0` の EMA 評価が 19:35:32Z に terminal かつ success になり
watcher は hook を起動したが、その log は
*"this workspace has not been trusted"* の 1 行だけである。`/home/islab/...`
という綴りで trust dialog が受理されていないため、headless の Bash 呼び出しが
すべて拒否される。**この状態が続くかぎり、終了した run は記録されずに
溜まりつづける**——8 つの失敗ディレクトリが消えたのと同じ経路である。

`chosen` は依然 null であり、決めるのは人間である。この postrun は何も
起動していないし、何も止めていない。

## 追記（2026-09-10 20:47Z）— 同じ arm の隣の seed は、1 回目で完走した

`cifar10_r18_adr-s2` の model 重み評価（`eval-5709dc2e3701675af39e`、lane U）が
**1 回目の試行で `exit=0`** になった。19:45Z の追記が扱った `-s1` と同じ arm・
同じ config・同じ checkpoint 種別で、違うのは seed だけである。数字は plan 0097
の 20:47Z エントリにある。

分かったことは 4 つある。どれも証拠であって、選択ではない。

1. **Option B の再現が 2 本目になった。** `created_at 19:36:21.081Z` →
   `finished_at 20:39:10.159Z` で **1 時間 2 分 49 秒**。AutoAttack 自身の
   stage 時間の合計 3,737.0 秒が壁時計の **99.2 %** を占め、2 つの checkpoint の
   4 stage を「その stage に実際に入った入力数」で割ると **6.54 / 64.4 /
   150.8 / 166.9 ms** 対 **6.53 / 65.0 / 153.3 / 169.5 ms**——**4 stage すべてで
   差 1.7 % 以下**である。19:45Z の `-s1`（1 時間 4 分 9 秒）と合わせて、
   arm 3 の専有時の AutoAttack 費用は **≒1 時間 3 分 ± 40 秒** で 2 回測れた。

2. **「2 本同時でも危険ではない」がもう 1 例出た。** `canon-eval-lane-V.log` が
   `r18_trades_adr-s1` の EMA 評価を **19:36:18Z——この run の 3 秒前**に開始し、
   **20:42:32Z に `exit=0`**（1 時間 6 分 14 秒）で終えた。壁時計上ほぼ完全に
   重なっているのに **どちらも遅くなっていない**。19:45Z の追記の結論
   ——**危険なのは「同時に 2 本」ではなく「1 枚の device に 2 本」**——の、
   独立した 2 例目である。これも累積時間マークと timestamp からの再構成であり、
   process table からの読み取りではない。

3. **推定量の照合は 18 件になり、上限は動かない。** 印字された robust accuracy と
   `src/ard/evaluation/autoattack.py:213` の非バッチ再計算値の比較は、この run で
   `best.pt` **48.22 % 対 48.22 %（2 度目の完全一致）**、`last.pt` **44.82 % 対
   44.84 %**（+2 枚、+0.02 pp）。通算 **18 件中 13 件が不一致で、13 件すべて
   同じ向き**（記録側が高い）、最大幅は依然 **0.09 pp** である。plan 0097 の
   20:47Z エントリが報告するどの差よりも 1 桁小さく、どちらの推定量を使っても
   結論は変わらない。

4. **証拠が消える経路が、また 1 回動いた。** `canon-eval-lane-X.log` が
   20:42:59Z に `r18_trades-s1` の評価を開始しようとして
   `FileExistsError: refusing to overwrite existing evaluation output` で
   `exit=1` になり、その **16 秒後**の 20:43:18Z に同じ lane が同じパスで
   開始に成功している。つまりその間に **11:25:07Z に始まった lane A の試行の
   出力ディレクトリが削除された**。16:16Z・18:32Z の追記が記録したのと同じ
   パターンで、通算 3 例目である。

**`chosen: null` のまま起動された scientific job は 9 件目と 10 件目になった。**
`canon-eval-lane-W.log:1` が `r18_adr-s2` の EMA 評価を、この run が GPU を
空けた **60 秒後**の 20:40:11Z に開始し、上記 lane X が 20:43:18Z に続いた。
**この postrun は何も起動していないし、何も止めていない**；他の session の job を
止めるのは人間の判断である。

**headless postrun は依然として動いていない。** この run の hook log
（`20260910T203921Z-eval-5709dc2e…_evaluation.log`）と `r18_trades_adr-s1` EMA の
hook log（`20260910T204254Z-eval-533e04a8…_evaluation-ema.log`）は、どちらも
*"this workspace has not been trusted"* の 1 行だけである。直し方は
`/home/shunsukenaito/.claude.json` の
`projects["/home/islab/workspace-local/shunsuke.naito/ard_codex_bootstrap"].hasTrustDialogAccepted`
を `true` にするか、その綴りで一度対話的に trust dialog を受理することだが、
**これは user 側の設定変更なので、この postrun は行っていない**。

`chosen` は依然 null であり、決めるのは人間である。

## 追記（2026-09-10 21:48Z）— 同じ seed の EMA 側も 1 回目で完走した

`cifar10_r18_adr-s2` の EMA 重み評価（`eval-669190483234c84c27f1`、lane W）が
**1 回目の試行で `exit=0`** になった。20:47Z の追記が扱った同じ seed の model 側
と、違うのは評価した checkpoint の種別だけである。数字は plan 0097 の 21:48Z
エントリにある。

分かったことは 4 つある。どれも証拠であって、選択ではない。

1. **Option B の再現が 3 本目になった。** `created_at 20:40:14.085Z` →
   `finished_at 21:43:34.709Z` で **1 時間 3 分 20.6 秒**。AutoAttack 自身の
   stage 時間の合計 3,768.7 秒が壁時計の **99.2 %** を占め、2 つの checkpoint の
   4 stage を「その stage に実際に入った入力数」で割ると **6.54 / 64.3 /
   152.2 / 165.9 ms** 対 **6.50 / 64.5 / 152.0 / 168.0 ms**——**4 stage すべてで
   差 1.3 % 以下**である。arm 3 の専有時の AutoAttack 費用は、これで
   **≒1 時間 3 分 ± 40 秒** で 3 回測れたことになる（s1 model 1 時間 4 分 9 秒、
   s2 model 1 時間 2 分 49 秒、s2 EMA 1 時間 3 分 21 秒）。**10 回の OOM を出した
   のと同じ大きさの入力が、専有さえしていれば 3 回続けて 1 時間強で終わっている。**

2. **「2 本同時でも危険ではない」が 3 例目になった。** `canon-eval-lane-X.log` が
   `r18_trades-s1` の model 評価を **20:43:18Z から 21:47:15Z**（`exit=0`）まで
   走らせ、この run のほぼ全区間と重なっている。それでも **どちらも遅くなって
   いない**——この run は専有時の速度から 1.3 % しかずれておらず、lane X の
   APGD-CE の単価（55.1 秒 ÷ 8,276 枚 = 6.66 ms）もこの run と同じ水準である。
   **危険なのは「同時に 2 本」ではなく「1 枚の device に 2 本」**という 19:45Z の
   結論の、独立した 3 例目である。これも累積時間マークと timestamp からの
   再構成であり、process table からの読み取りではない。

3. **推定量の照合は 20 件になり、上限は動かない。** 印字された robust accuracy と
   `src/ard/evaluation/autoattack.py:213` の非バッチ再計算値の比較は、この run で
   `best-ema.pt` **49.27 % 対 49.30 %**（+3 枚、+0.03 pp）、`last.pt`
   **45.00 % 対 45.03 %**（+3 枚、+0.03 pp）。通算 **20 件中 15 件が不一致で、
   15 件すべて同じ向き**（記録側が高い）、最大幅は依然 **0.09 pp** である。
   plan 0097 の 21:48Z エントリが報告するどの差よりも 1 桁小さく、どちらの
   推定量を使っても結論は変わらない。

4. **証拠が消える経路が、また 1 回動いた。通算 4 例目である。**
   `r18_trades_adr-s0` の EMA 評価は 11:24:11Z に lane A が開始し、その 56 秒後の
   **11:25:07Z に `exit=143`（SIGTERM）で死んでいた**。bundle はそれ以来ずっと
   `running` と読めていたが、実体は 10 時間以上前に死んだ job である。
   21:44:08Z に lane Y が同じパスで開始し、**`FileExistsError` を出さずに走り
   出した**——つまりその間に、死んだ試行の出力ディレクトリが削除されている。

**`chosen: null` のまま起動された scientific job は 11 件目になった。**
上記の lane Y が、この run が GPU を空けた **33 秒後**の 21:44:08Z に開始している。
**この postrun は何も起動していないし、何も止めていない**；他の session の job を
止めるのは人間の判断である。

**headless postrun は依然として動いていない。** この run の hook log
（`20260910T214345Z-eval-669190483234c84c27f1_evaluation-ema.log`）と
`r18_trades-s1` model の hook log
（`20260910T214718Z-eval-01e527cf…_evaluation.log`）は、どちらも
*"this workspace has not been trusted"* の 1 行だけである。直し方は 20:47Z の
追記と同じで、**これは user 側の設定変更なので、この postrun は行っていない**。

`chosen` は依然 null であり、決めるのは人間である。

## 追記（2026-09-10 22:55Z）— arm 5 seed 0 の EMA 評価が完走した

`cifar10_r18_trades_adr-s0` の EMA 重み評価（`eval-77f66adc6ec58702c412`、lane Y）が
`exit=0` で終わった。この bundle にとっては 2 回目の試行で、1 回目（lane A、
11:24:11Z 開始）は 56 秒後に SIGTERM で止まっていた。数字は plan 0097 の
22:55Z エントリにある。

分かったことは 4 つある。どれも証拠であって、選択ではない。

1. **専有時の費用を arm 5 で初めて測れた。** `created_at 21:44:10.347Z` →
   `finished_at 22:49:41.819Z` で **1 時間 5 分 31.5 秒**。AutoAttack 自身の
   stage 時間の合計 3,899.5 秒が壁時計の **99.2 %** を占める。2 つの checkpoint の
   4 stage を「その stage に実際に入った入力数」で割ると **6.52 / 63.9 /
   151.3 / 167.2 ms** 対 **6.52 / 63.7 / 151.3 / 167.0 ms** で、**4 stage すべてで
   差 0.3 % 以下**である。arm 3 の 3 回（≒1 時間 3 分 ± 40 秒）より約 2 分長い。

2. **「2 本同時でも危険ではない」が 4 例目になった。** `canon-eval-lane-Z.log` が
   `r18_trades_adr-s2` の model 評価を **21:47:47Z** に開始し、この run が終わった
   時点でもまだ走っていた。ほぼ 1 時間まるごと重なっていたのに、この run の
   単価は専有時と 0.3 % しか違わない。**危険なのは「同時に 2 本」ではなく
   「1 枚の device に 2 本」**という 19:45Z の結論の、独立した 4 例目である。
   累積時間マークと timestamp からの再構成であり、process table からの読み取り
   ではない。

3. **推定量の照合は 22 件になり、上限は動かない。** 印字された robust accuracy と
   `src/ard/evaluation/autoattack.py:213` の非バッチ再計算値の比較は、この run で
   `best-ema.pt` **49.51 % 対 49.54 %**（+3 枚、+0.03 pp）、`last.pt`
   **48.34 % 対 48.36 %**（+2 枚、+0.02 pp）。通算 **22 件中 17 件が不一致で、
   17 件すべて同じ向き**（記録側が高い）、最大幅は依然 **0.09 pp** である。

4. **証拠が消える経路が、さらに 2 回動いた（通算 5・6 例目）。** lane AA は
   22:50:26Z に `r18_trades-s2` の model 評価を、lane B の 11:10:21Z の試行
   （12:54:04Z に SIGTERM）が使っていたパスで、`FileExistsError` を出さずに
   開始した。lane Z も、lane I の 09:09:31Z の試行が使っていたパスで同じように
   開始した。どちらも、その間に古い出力ディレクトリが削除されたことになる。

**`chosen: null` のまま起動された scientific job は 12 件目と 13 件目になった。**
lane Z（21:47:47Z、lane X が GPU を空けた 32 秒後。21:48Z の追記は数えていない）と
lane AA（22:50:26Z、この run が GPU を空けた 44 秒後）である。
**この postrun は何も起動していないし、何も止めていない。**

**訂正: headless postrun は動いている。** 19:45Z・20:47Z・21:48Z の追記は
「trust されていないので headless session の Bash はすべて拒否される」と書いたが、
これは誤りだった。`scripts/ardx/postrun_hook.sh:168-169` は allowlist を
`--allowedTools` で直接渡しているので、trust の警告が捨てるのは settings
ファイル側の写しだけで、allowlist にあるコマンドは動く。21:47:18Z の
`eval-01e527cf…` の headless postrun は 70 turn 動いて終了状態を確かめ、
別の session がちょうど plan とこの packet を編集していたので書き込みを見送っていた。
この追記は headless postrun が書いた最初のものである。trust の設定変更は、
headless postrun のためには不要である。

`chosen` は依然 null であり、決めるのは人間である。

## 参照

- plan: `docs/plans/0097-adr-cifar10-replication.md`（Progress log、2026-09-10 11:01Z）
- 証拠ログ: `runs/adr-cifar10-campaign-v1/canon-eval-lane-{D,E,F}.log`
  （失敗した評価ディレクトリ自体は 11:05Z の時点で既に削除されている）
- 該当コード: `src/ard/evaluation/autoattack.py:211-213`、
  `src/ard/cli/evaluate.py:411-421`（いずれも pinned SHA
  `cd0b571e4685` と hash 一致）
