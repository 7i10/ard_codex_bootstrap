campaign_id: c10-r18-ws1-b128-core-s0-v1
git_sha: null
execution_profile:
  id: ws1_prb128_gb128_localbn_v1
  world_size: 1
  per_rank_batch_size: 128
  global_batch_size: 128
  batchnorm_mode: local_per_rank
external_process_policy: allow_with_memory_gate
hosts:
  hamster: {gpus: [0, 1]}
  ferret: {gpus: [0, 1, 2]}
reservations:
  - host: ferret
    gpu: 0
    run_id: chen-rslad-production-s0-0ca90ad
    execution_profile: ws2_prb64_gb128_localbn
    protected_git_sha: 0ca90ad3d48fe019151363b00c6da2160d64eb99
    active: true
    release_marker: /home/shunsukenaito/workspace-local/ard-runs/ard_codex_bootstrap/chen-rslad-production-s0-0ca90ad/control/campaign-release-ready.json
  - host: ferret
    gpu: 1
    run_id: chen-rslad-production-s0-0ca90ad
    execution_profile: ws2_prb64_gb128_localbn
    protected_git_sha: 0ca90ad3d48fe019151363b00c6da2160d64eb99
    active: true
    release_marker: /home/shunsukenaito/workspace-local/ard-runs/ard_codex_bootstrap/chen-rslad-production-s0-0ca90ad/control/campaign-release-ready.json
jobs:
  - id: prod-h0-chen-rslad-s0
    host: hamster
    gpu: 0
    teacher: chen2021_ltd_wrn34_10
    method: rslad
    seed: 0
    config: configs/production/single_gpu/cifar10_r18_rslad_chen.yaml
    output: production/chen-rslad-s0
    wandb: {entity: shunsuke-n-waseda-university, project: single-teacher-ard, group: chen-cifar10-r18-ws1, run_id: prod-chen-rslad-s0}
    phases:
      train: ["{PYTHON}", -m, ard.cli.train, --config, "{CONFIG_PATH}", --output, "{JOB_OUTPUT_DIR}"]
      pgd_evaluate: ["{PYTHON}", -m, ard.cli.evaluate, --config, "{CONFIG_PATH}", --checkpoint-dir, "{JOB_OUTPUT_DIR}", --output, "{JOB_OUTPUT_DIR}/evaluation-pgd"]
      autoattack: ["{PYTHON}", -m, ard.cli.evaluate, --config, "{CONFIG_PATH}", --checkpoint-dir, "{JOB_OUTPUT_DIR}", --output, "{JOB_OUTPUT_DIR}/evaluation-autoattack", --allow-autoattack, evaluation.autoattack=true]
    priority: 4
    core: true
    pilot_peak_reserved_mib: 6000
  - id: prod-h0-bart-student-s0
    host: hamster
    gpu: 0
    teacher: bartoldson2024_adversarial_wrn94_16
    method: rslad_student
    seed: 0
    config: configs/production/single_gpu/cifar10_r18_rslad_student_bartoldson.yaml
    output: production/bartoldson-student-s0
    wandb: {entity: shunsuke-n-waseda-university, project: single-teacher-ard, group: bartoldson-cifar10-r18-ws1, run_id: prod-bart-student-s0}
    phases:
      train: ["{PYTHON}", -m, ard.cli.train, --config, "{CONFIG_PATH}", --output, "{JOB_OUTPUT_DIR}"]
      pgd_evaluate: ["{PYTHON}", -m, ard.cli.evaluate, --config, "{CONFIG_PATH}", --checkpoint-dir, "{JOB_OUTPUT_DIR}", --output, "{JOB_OUTPUT_DIR}/evaluation-pgd"]
    priority: 7
    core: true
    pilot_peak_reserved_mib: 10000
  - id: prod-h1-bart-entropy-s0
    host: hamster
    gpu: 1
    teacher: bartoldson2024_adversarial_wrn94_16
    method: rslad_entropy
    seed: 0
    config: configs/production/single_gpu/cifar10_r18_rslad_entropy_bartoldson.yaml
    output: production/bartoldson-entropy-s0
    wandb: {entity: shunsuke-n-waseda-university, project: single-teacher-ard, group: bartoldson-cifar10-r18-ws1, run_id: prod-bart-entropy-s0}
    phases:
      train: ["{PYTHON}", -m, ard.cli.train, --config, "{CONFIG_PATH}", --output, "{JOB_OUTPUT_DIR}"]
      pgd_evaluate: ["{PYTHON}", -m, ard.cli.evaluate, --config, "{CONFIG_PATH}", --checkpoint-dir, "{JOB_OUTPUT_DIR}", --output, "{JOB_OUTPUT_DIR}/evaluation-pgd"]
      autoattack: ["{PYTHON}", -m, ard.cli.evaluate, --config, "{CONFIG_PATH}", --checkpoint-dir, "{JOB_OUTPUT_DIR}", --output, "{JOB_OUTPUT_DIR}/evaluation-autoattack", --allow-autoattack, evaluation.autoattack=true]
    priority: 2
    core: true
    pilot_peak_reserved_mib: 10000
  - id: prod-h1-chen-entropy-s0
    host: hamster
    gpu: 1
    teacher: chen2021_ltd_wrn34_10
    method: rslad_entropy
    seed: 0
    config: configs/production/single_gpu/cifar10_r18_rslad_entropy_chen.yaml
    output: production/chen-entropy-s0
    wandb: {entity: shunsuke-n-waseda-university, project: single-teacher-ard, group: chen-cifar10-r18-ws1, run_id: prod-chen-entropy-s0}
    phases:
      train: ["{PYTHON}", -m, ard.cli.train, --config, "{CONFIG_PATH}", --output, "{JOB_OUTPUT_DIR}"]
      pgd_evaluate: ["{PYTHON}", -m, ard.cli.evaluate, --config, "{CONFIG_PATH}", --checkpoint-dir, "{JOB_OUTPUT_DIR}", --output, "{JOB_OUTPUT_DIR}/evaluation-pgd"]
      autoattack: ["{PYTHON}", -m, ard.cli.evaluate, --config, "{CONFIG_PATH}", --checkpoint-dir, "{JOB_OUTPUT_DIR}", --output, "{JOB_OUTPUT_DIR}/evaluation-autoattack", --allow-autoattack, evaluation.autoattack=true]
    priority: 6
    core: true
    pilot_peak_reserved_mib: 6000
  - id: prod-f0-bart-rslad-s0
    host: ferret
    gpu: 0
    teacher: bartoldson2024_adversarial_wrn94_16
    method: rslad
    seed: 0
    config: configs/production/single_gpu/cifar10_r18_rslad_bartoldson.yaml
    output: production/bartoldson-rslad-s0
    wandb: {entity: shunsuke-n-waseda-university, project: single-teacher-ard, group: bartoldson-cifar10-r18-ws1, run_id: prod-bart-rslad-s0}
    phases:
      train: ["{PYTHON}", -m, ard.cli.train, --config, "{CONFIG_PATH}", --output, "{JOB_OUTPUT_DIR}"]
      pgd_evaluate: ["{PYTHON}", -m, ard.cli.evaluate, --config, "{CONFIG_PATH}", --checkpoint-dir, "{JOB_OUTPUT_DIR}", --output, "{JOB_OUTPUT_DIR}/evaluation-pgd"]
      autoattack: ["{PYTHON}", -m, ard.cli.evaluate, --config, "{CONFIG_PATH}", --checkpoint-dir, "{JOB_OUTPUT_DIR}", --output, "{JOB_OUTPUT_DIR}/evaluation-autoattack", --allow-autoattack, evaluation.autoattack=true]
    priority: 1
    core: true
    pilot_peak_reserved_mib: 10000
  - id: prod-f0-chen-student-s0
    host: ferret
    gpu: 0
    teacher: chen2021_ltd_wrn34_10
    method: rslad_student
    seed: 0
    config: configs/production/single_gpu/cifar10_r18_rslad_student_chen.yaml
    output: production/chen-student-s0
    wandb: {entity: shunsuke-n-waseda-university, project: single-teacher-ard, group: chen-cifar10-r18-ws1, run_id: prod-chen-student-s0}
    phases:
      train: ["{PYTHON}", -m, ard.cli.train, --config, "{CONFIG_PATH}", --output, "{JOB_OUTPUT_DIR}"]
      pgd_evaluate: ["{PYTHON}", -m, ard.cli.evaluate, --config, "{CONFIG_PATH}", --checkpoint-dir, "{JOB_OUTPUT_DIR}", --output, "{JOB_OUTPUT_DIR}/evaluation-pgd"]
    priority: 8
    core: true
    pilot_peak_reserved_mib: 6000
  - id: prod-f1-bart-joint-s0
    host: ferret
    gpu: 1
    teacher: bartoldson2024_adversarial_wrn94_16
    method: rslad_joint
    seed: 0
    config: configs/production/single_gpu/cifar10_r18_rslad_joint_bartoldson.yaml
    output: production/bartoldson-joint-s0
    wandb: {entity: shunsuke-n-waseda-university, project: single-teacher-ard, group: bartoldson-cifar10-r18-ws1, run_id: prod-bart-joint-s0}
    phases:
      train: ["{PYTHON}", -m, ard.cli.train, --config, "{CONFIG_PATH}", --output, "{JOB_OUTPUT_DIR}"]
      pgd_evaluate: ["{PYTHON}", -m, ard.cli.evaluate, --config, "{CONFIG_PATH}", --checkpoint-dir, "{JOB_OUTPUT_DIR}", --output, "{JOB_OUTPUT_DIR}/evaluation-pgd"]
      autoattack: ["{PYTHON}", -m, ard.cli.evaluate, --config, "{CONFIG_PATH}", --checkpoint-dir, "{JOB_OUTPUT_DIR}", --output, "{JOB_OUTPUT_DIR}/evaluation-autoattack", --allow-autoattack, evaluation.autoattack=true]
    priority: 3
    core: true
    pilot_peak_reserved_mib: 10000
  - id: prod-f2-chen-joint-s0
    host: ferret
    gpu: 2
    teacher: chen2021_ltd_wrn34_10
    method: rslad_joint
    seed: 0
    config: configs/production/single_gpu/cifar10_r18_rslad_joint_chen.yaml
    output: production/chen-joint-s0
    wandb: {entity: shunsuke-n-waseda-university, project: single-teacher-ard, group: chen-cifar10-r18-ws1, run_id: prod-chen-joint-s0}
    phases:
      train: ["{PYTHON}", -m, ard.cli.train, --config, "{CONFIG_PATH}", --output, "{JOB_OUTPUT_DIR}"]
      pgd_evaluate: ["{PYTHON}", -m, ard.cli.evaluate, --config, "{CONFIG_PATH}", --checkpoint-dir, "{JOB_OUTPUT_DIR}", --output, "{JOB_OUTPUT_DIR}/evaluation-pgd"]
      autoattack: ["{PYTHON}", -m, ard.cli.evaluate, --config, "{CONFIG_PATH}", --checkpoint-dir, "{JOB_OUTPUT_DIR}", --output, "{JOB_OUTPUT_DIR}/evaluation-autoattack", --allow-autoattack, evaluation.autoattack=true]
    priority: 5
    core: true
    pilot_peak_reserved_mib: 6000
