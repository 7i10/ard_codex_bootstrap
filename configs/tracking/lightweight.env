# W&B identity for plan 0103 (lightweight-model ImageNet adversarial training),
# kept separate from the ARD project (production.env -> single-teacher-ard).
# Production runs (Phase 0 2x2, Phase 1 full runs, Phase 2 method comparison):
WANDB_ENTITY=shunsuke-n-waseda-university
WANDB_PROJECT=lightweight-imagenet-at
# Exploration/diagnostic runs (canaries, preprocessing checks, throwaway probes)
# use WANDB_PROJECT=lightweight-imagenet-at-dev instead.
